# JARVIS V8 — Dependencias
# Instalar:python -m pip install -r requirements.txt

# Core (obligatorias)
flask>=3.0
flask-socketio>=5.3
flask-cors>=4.0
psutil>=5.9
requests>=2.31
anthropic>=0.40
python-dotenv>=1.0

# Voz (opcionales — ambient mode)
# SpeechRecognition>=3.10
# pyttsx3>=2.90
# pyaudio>=0.2.14

# Desktop control Windows (opcional)
# pywin32>=306
# pyautogui>=0.9.54
